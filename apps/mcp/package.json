{
  "name": "arp-mcp",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "sideEffects": false,
  "scripts": {
    "build": "esbuild src/index.ts --bundle --platform=node --format=esm --packages=external --alias:arp-domain=../../packages/domain/src/index.ts --outfile=dist/server.mjs",
    "dev": "esbuild src/local.ts --bundle --platform=node --format=esm --packages=external --alias:arp-domain=../../packages/domain/src/index.ts --outfile=dist/local.mjs && node dist/local.mjs",
    "typecheck": "tsc"
  },
  "dependencies": {
    "@clerk/backend": "^2.33.0",
    "@modelcontextprotocol/sdk": "^1.26.0",
    "arp-domain": "workspace:*",
    "arp-http": "workspace:*",
    "express": "^5.1.0",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/express": "^5.0.0",
    "@types/node": "^22.10.0",
    "@types/supertest": "^6.0.0",
    "esbuild": "^0.21.5",
    "supertest": "^7.0.0",
    "typescript": "^5.6.3"
  },
  "engines": {
    "node": ">=20"
  }
}
